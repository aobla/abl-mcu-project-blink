#
# Example config file for OpenOCD from /usr/local/share/openocd/scripts/board/stm32h7x3i_eval.cfg
#
source [find interface/stlink.cfg]

# #st-link
transport select hla_swd

# Work-area is a space in RAM used for flash programming
# STM32H743xx RAM_D1: 512 KB
set WORKAREASIZE 0x80000
set CHIPNAME STM32H734VI
set BOARDNAME DEVEBOX_STM32H7XX_M_V2

source [find target/stm32h7x.cfg]

# Use connect_assert_srst here to be able to program
# even when core is in sleep mode
# reset_config srst_only srst_nogate connect_assert_srst
reset_config separate

$_CHIPNAME.cpu0 configure -event gdb-attach {
        echo "Debugger attaching: halting execution"
        gdb breakpoint_override hard
}

$_CHIPNAME.cpu0 configure -event gdb-detach {
        echo "Debugger detaching: resuming execution"
        resume
}

# Due to the use of connect_assert_srst, running gdb requires
# to reset halt just after openocd init.
# rename init old_init
# proc init {} {
#         old_init
#         reset halt
# }
